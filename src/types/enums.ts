export enum Gender {
    Male = 'Male',
    Female = 'Female',
    Unknown = 'unknown',
    N_A = 'n/a',
}

export enum Order {
    ASC = 'asc',
    DESC = 'desc',
}

export enum Role {
    USER,
    ADMIN,
}
